// Import any required models here
// const Example = require("../models/example");

// Define your service methods
exports.getExamples = async (req, res) => {
  res.send("When models are entered just find the collections here");
  // return await Example.find();
};

exports.createExample = async (name) => {
  // const example = new Example({ name })
  console.log("When models are entered just find the collections here");
  // return await example.save();
};
